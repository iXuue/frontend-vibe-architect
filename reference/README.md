# Deprecated Reference Layer

The `reference/` folder is an early V1 draft layer.

The active skill structure has moved to:

- `flows/`
- `tracks/`
- `modes/`
- `rules/`
- `output/`
- `examples/`

Agents should not read `reference/` first when using this skill.

Use the active layered structure through `SKILL.md` instead.

This folder is retained for historical context only. Do not delete it without separate explicit user confirmation.
